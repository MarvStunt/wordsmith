services:
    db:
        build: ./db
        container_name: db
    
    web:
        build: ./web
        ports:
          - "8080:80"
        container_name: web
        depends_on:
          - words
        volumes:
          - ./web/static:/app/static

    words:
        build: ./words
        container_name: words
        depends_on:
          - db